#include<stdio.h>
void main()
{
    int pin;
    float attendence;
    char s;
    char a[10];
    printf("size occcupide by  integer data type=%d\n",sizeof(pin));
    printf("size occcupide by  float data type=%d\n",sizeof(attendence));
    printf("size occcupide by  character data type=%d\n",sizeof(s));
    printf("size occcupide by string data type=%d\n",sizeof(a[10]));
}